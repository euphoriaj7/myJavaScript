// example of a function
let averageTwoNubmers = function(firstNum, secondNum) {
    let sum = firstNum + secondNum;
    let avg = sum / 2;
    return avg;
}

// 2nd exmaple of another function 
let averageTwoNumbers = (firstNum, secondNum) => {
    let sum = firstNum + secondNum;
    let avg = sum / 2;
    return avg;
}

// third example
function averageTwoNubmers(firstNum, secondNum) {
    let sum = firstNum + secondNum;
    let avg = sum / 2;
    return avg;
}